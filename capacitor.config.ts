import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rancho.distribucionz',
  appName: 'DistribucionRancho',
  webDir: 'www'
};

export default config;
